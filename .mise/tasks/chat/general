#!/usr/bin/env bash
#MISE description="Chat interactif avec l'agent général (gemma4:26b)"
set -euo pipefail
REPO_DIR="${MISE_PROJECT_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/../../.." && pwd)}"
exec python3 "$REPO_DIR/scripts/chat.py" general
